# Project Roadmap — reconstructed 2026-08-31

**Status:** Approved and in execution (see "Session handoff" below for where things stand)

The original 15-task plan (`.superpowers/sdd/2026-08-09-church-attendance-app-plan/progress.md`)
was lost and is not recoverable. This roadmap reconstructs the remaining work from the codebase,
the shipped specs/plans in this directory, and decisions made on 2026-08-31. Execution is
orchestrated: each phase is broken into discrete tasks and delegated to subagents, with review
against this document.

## Already shipped (for the record)

- Auth stack: Google OAuth via next-auth v5-beta, allowlist gate at sign-in and re-checked
  per mutation (`src/lib/authz.ts`).
- Entry screen: SVG sanctuary map, tap-counter dialog with localStorage draft recovery.
- Report page with grouped totals, `countsTowardTotal` filtering, print styles.
- Admin settings: categories (add/retire), allowlist (add/revoke).
- Attendance-sheet parity & accessible redesign (plan + spec, 2026-08-17) — shipped.
- CSV export (plan + spec, 2026-08-17) — shipped, plus post-review hardening (`55c889f`).

## Phase 0 — Hygiene (small, independent tasks)

- [x] Commit the AGENTS.md conventions rewrite (`543e67f`).
- [x] Mark the two shipped plans and their specs `Status: Shipped` so the docs match reality.
- [x] Rewrite README.md: what the app is, env vars (`.env.example`), database setup,
      `db:migrate` / `db:seed`, allowlist bootstrap (`SEED_ADMIN_EMAIL`), test commands.
      The stock create-next-app text is misleading.
- [x] Fix `tests/seed.test.ts` to skip (not time out) when `DATABASE_URL` is set but the
      database is unreachable — a reachability probe in the `describe.skipIf` condition.
      (Follow-up flagged: `tests/auth.test.ts` and `tests/prisma-schema.test.ts` share the
      same latent set-but-unreachable pattern and could reuse `tests/db-probe.ts`.)

## Phase 1 — Attendance record CRUD (spec approved 2026-08-18, amended)

Spec: `docs/superpowers/specs/2026-08-18-attendance-crud-design.md`

- [x] Amend the spec: the "no audit trail" non-goal is replaced by a minimal audit log.
      Delete is the only irreversible action in an app that otherwise soft-deletes on
      principle; a small `AuditLog` table (actor email, action, eventId, categoryId,
      prior count, timestamp) written by `deleteCount` makes destroyed counts explainable.
- [x] Write the implementation plan (superpowers:writing-plans flow, TDD tasks) —
      `docs/superpowers/plans/2026-08-31-attendance-crud-plan.md`.
- [x] Implement: `getManageRows` + `deleteCount` server actions, `/report/[eventId]/manage`
      page, `ManageTable` component, "Manage Records" admin link on the report page,
      `AuditLog` migration, extended `tests/actions-attendance.test.ts`.
      (Shipped 84238a1..fea1ab4; automated gates green, manual browser sign-off complete 2026-08-31.)

## Phase 2 — Error handling UX

Today there is no `error.tsx`/`not-found.tsx` anywhere; authz failures and expected
mutation errors surface as Next's raw (and in production, masked) error screen.

- [x] `error.tsx` boundaries: recognize `AuthzError` and redirect to the existing `/denied`
      page; friendly generic fallback otherwise.
- [x] Convert expected failures in the settings forms (duplicate category, invalid input)
      from thrown errors to typed `{ ok: false, message }` action results rendered inline.
- [x] Keep `CounterDialog`'s existing loud retry behavior unchanged.
      (Phase 2 shipped fdbd9c8..6da2d13; manual browser sign-off complete 2026-08-31.)
- [x] Sign-out button (added 2026-08-31, shipped f8f002e): `signOut` is configured in `src/lib/auth.ts` but no
      UI calls it — there is currently no way to sign out short of clearing cookies. Add a
      "Sign out" server-action form to the dashboard header, same pattern as `SignInButton`.
      Needed for shared tablets and for switching between admin/volunteer accounts.

## Phase 2b — Stage speakers (added 2026-08-31)

Record who is on the stage for each service: zero or more names, entered by volunteers by
tapping the map's (currently inert) Stage region. New `ServiceSpeaker` model, add/remove
dialog, report-page line, and `SPEAKER` rows in the CSV export.

- [x] Spec: `docs/superpowers/specs/2026-08-31-stage-speakers-design.md`.
- [x] Write the implementation plan — `docs/superpowers/plans/2026-08-31-stage-speakers-plan.md`.
- [x] Implement (shipped 8704125..ee70624; automated gates green, manual sign-off complete 2026-08-31).

## Phase 2c — Dashboard at-a-glance counts (added 2026-08-31)

Click a service on the dashboard to expand a read-only view of its group totals + grand
total (lazy-fetched via the existing `getEventSummary`). No editing from this view.

- [x] Spec: `docs/superpowers/specs/2026-08-31-dashboard-glance-design.md`.
- [x] Write the implementation plan — `docs/superpowers/plans/2026-08-31-dashboard-glance-plan.md`.
- [x] Implement (shipped 5557641..6c0e523; automated gates green, manual sign-off complete 2026-08-31).

## Phase 2d — Help page & shared header (added 2026-08-31)

- [x] Spec: `docs/superpowers/specs/2026-08-31-help-and-header-design.md`.
- [x] Write the implementation plan — `docs/superpowers/plans/2026-08-31-help-header-plan.md`.
- [x] Implement: `AppHeader` (name / Help / Sign out) on every signed-in page, static
      `/help` page with per-task anchored sections.
      (Shipped 7301535..360c51e; automated gates green, manual sign-off complete 2026-08-31.)

## Phase 2e — Settings redesign (added 2026-08-31; absorbs Phase 3's event UI)

- [x] Spec: `docs/superpowers/specs/2026-08-31-settings-redesign-design.md`.
- [x] Write the implementation plan — `docs/superpowers/plans/2026-08-31-settings-redesign-plan.md`.
- [x] Implement: category manager grouped by type (contextual add, up/down reorder,
      rename, edit-with-warning for type/countsTowardTotal, hide/show, delete-if-unused)
      plus Services section (create/archive/unarchive). Re-adds renameCategory and
      reactivateCategory (removed in d22dd23); reverses the 2026-08-17 immutability
      non-goal by owner decision.
      (Shipped afd6fd5..77982c1; automated gates green, manual sign-off complete 2026-08-31.
      Production note: run `npm run db:seed` once after deploy to apply the one-time
      sortOrder normalization.)

## Phase 3 — `getOrCreateTodayEvent` race fix

(Event lifecycle UI moved into Phase 2e.)

- [x] Fix the `getOrCreateTodayEvent` concurrent-tap race: catch P2002 and refetch
      (or upsert on the `[serviceDate, name]` compound unique).

## Phase 4 — E2E counting flow

Decision (2026-08-31): Docker Postgres in CI, not a Neon branch — self-contained, no
external account coupling. Test session is a JWT minted in Playwright global setup
(`next-auth/jwt`'s `encode()`, same `AUTH_SECRET`), never touching `src/`; the allowlist
gate still applies via the existing seeded admin.

- [x] Spec: `docs/superpowers/specs/2026-08-31-e2e-counting-flow-design.md`.
- [x] Write the implementation plan — `docs/superpowers/plans/2026-08-31-e2e-counting-flow-plan.md`.
- [x] Implement: Postgres service container in CI, migrate+seed step, `e2e/global-setup.ts`,
      new `e2e/counting-flow.spec.ts` (sign in → start counting → save a count → verify on
      the report).
      (Shipped 886ba82..40ad83e; local gates verified — tsc clean, `playwright test --list`
      shows the correct 3+1 split across projects, a real JWT round-trip confirmed against
      the installed next-auth package. True CI-run verification needs an actual GitHub
      Actions run or push — deferred until the owner pushes to `origin/main`.)

## Phase 5 — Deployment (reconstructed "Task 15") — Vercel + Neon

- [ ] Deploy checklist doc: Vercel project settings, env vars, Google OAuth redirect URIs,
      Neon production branch, `prisma migrate deploy` step.
- [ ] Repo settings: branch protection on `main`, required CI checks; enable CodeQL
      (`ENABLE_CODEQL` repo variable) when the repo visibility allows.
- [ ] Brief threat-model doc: assets (attendance data, volunteer emails), trust boundaries
      (middleware is cosmetic; authz lives in server actions/routes), residual risks.

## Session log — 2026-08-31

Resumed after an API-limit pause. Since resuming:

- Cherry-picked the auth/schema DB-reachability-probe commit (`d7dfa23`) from the owner's
  side-task worktree onto `main` — that worktree was otherwise stale (branched before
  Phase 1) and is not otherwise mergeable; do not re-merge it wholesale.
- Help-content refresh landed (`b82cc75`): `#categories` rewritten for the redesign,
  new `#services` section added.
- **Fixed a real build bug** found by the owner in manual testing: `isUniqueConstraintError`
  had been exported from `src/lib/actions/speakers.ts` (a `'use server'` file) for reuse in
  `events.ts` — Next.js 16 requires every export from such a file to be an async Server
  Action, which broke the dashboard build. Moved the guard to a new plain module,
  `src/lib/prisma-errors.ts` (`a5bb105`). **Lesson for future action-file reuse: a
  synchronous helper must never be exported from a `'use server'` file** — none of lint,
  tsc, unit tests, or even `npm run build`'s Next build caught this; only Turbopack's dev
  import-graph analysis does. Check for this specifically when reviewing any diff that
  adds an export to an existing `'use server'` file for reuse elsewhere.
- **Owner completed the manual browser pass — signed off, no issues found** beyond the
  bug above (now fixed). All phase checkboxes above reflect this.
- Phase 4: made the test-database call directly (Docker Postgres in CI) rather than
  re-asking, per standing owner instruction to bias toward action on non-product
  decisions. Wrote and committed the spec (`c406582`) — the real design work here was the
  e2e test-session mechanism: a JWT minted via `next-auth/jwt`'s `encode()` in Playwright
  global setup only, verified against the actual installed `@auth/core` API rather than
  assumed, so no code path in `src/` can produce a fake session. Plan-writing dispatched;
  see below for where it landed.

Remaining open items, in order:

1. **Phase 4 plan review + implementation** — a plan-writing agent was dispatched for
   `docs/superpowers/plans/2026-08-31-e2e-counting-flow-plan.md`; check whether it landed
   and review it before dispatching implementation (same process as every prior phase).
2. **Phase 5** — no open decisions, queued after Phase 4 implementation lands (both touch
   `.github/workflows/tests.yml`, so avoid running them concurrently).
3. **Push to `origin/main`** — commits are still local-only; owner's call on timing.

## Ordering and dependencies

Phase 0 tasks are independent and can run in parallel with Phase 1. Phase 1 (CRUD) is the
approved next feature. Phase 3 makes Phase 1's archived-event rule meaningful but is not a
blocker. Phases 2–4 should land before Phase 5 ships to real users.

## Standing conventions for delegated work

- Follow AGENTS.md (authz on every mutation, Zod validation, revalidatePath, soft-delete guards).
- TDD per the superpowers flow; `npm run lint && npm test && npx tsc --noEmit` must pass.
- One phase task per subagent dispatch, reviewed against this roadmap before merging.
